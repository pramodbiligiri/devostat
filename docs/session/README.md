# docs/session/

Raw Claude Code session transcripts preserved as source material for the demo cast (`docs/demo.cast`) and for future demo regeneration.

## Files

| File | Date (IST) | Role |
|---|---|---|
| `ac2b52af-….jsonl` | 2026-04-09 16:14–18:51 | Pre-pivot — drafts plan-13 v1, triggers in-session v1→v2 nudge (tsconfig.json leak) |
| `eb521172-….jsonl` | 2026-04-10 10:28–12:33 | The v2→v3 pivot discussion — user proposes multi-module Maven restructure |
| `9680eb1a-….jsonl` | 2026-04-10 13:48–18:28 | Post-v3 execution continuation — not used in the demo narrative, preserved for completeness |

## Context

These transcripts document the plan-13 v1→v2→v3 pivot arc. The pivot commit is `b9e12d1` (tag: `plan-13-v3`, 2026-04-10 11:38 IST) — the point where plan-13 was restructured as a multi-module Maven project.

The demo cast (`docs/demo.cast`) was authored around the real events in `ac2b52af` and `eb521172`. The pre-pivot fictional cast is preserved separately under `docs/demo-1/demo.cast`.

These are raw Claude Code session logs (JSONL format). They are not intended to be diffed or read line-by-line — just preserved as reference material.

## Redactions

None. Task 9 (PII/secret scan) found 0 secrets or tokens across all three files. All hits were false positives (template placeholders, already-public git config email, `/home/pramod` paths already present elsewhere in the repo).
